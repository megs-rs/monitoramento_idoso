discovery:
  timeout: 5

# Credenciais ONVIF via arquivo .env na raiz do projeto:
#   MO_ONVIF_USER=admin
#   MO_ONVIF_PASS=sua_senha

# Credenciais Telegram via .env:
#   TELEGRAM_BOT_TOKEN=seu_token
#   TELEGRAM_CHAT_ID=seu_chat_id

manual_cameras:
  - ip: 192.168.31.117
    port: 10080
    name: "Câmera Principal"
#  - ip: 127.0.0.1
#    port: 0
#    name: "Webcam"
#    rtsp_url: "/dev/video0"

processing:
  model: "yolo11n.pt"
  confidence: 0.5
  reconnect_delay: 2

events:
  db_path: "data/events.db"
  clip_dir: "clips"
  clip_pre_seconds: 15
  clip_post_seconds: 15
